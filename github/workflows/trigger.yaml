name: Every 3 Minutes Workflow

on:
  schedule:
    - cron: '*/3 * * * *' # Runs every 3 minutes

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Print Current Time
      run: |
        echo "Current time is $(date)"
